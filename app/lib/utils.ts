// import { generateMnemonic } from "bip39";
// import { useRecoilState } from "recoil";
// import { mnemonicEth, mnemonicSol } from "../state/state";

// interface opt {
//   enum: "solana" | "ethereum";
// }

// const GenerateMnemonic = (opt: opt) => {
//   const [mnemonic, setMnemonic] = useRecoilState(
//     opt.enum === "solana" ? mnemonicSol : mnemonicEth
//   );
//   const mnemo = generateMnemonic();
//   setMnemonic(mnemo);
// };

// export default GenerateMnemonic;
